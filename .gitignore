# full-res originals stay local (huge); the built docs/ has the web sizes
/photos/
# moodboard references, not for publishing
/assets/
# old build output name
/dist/
.DS_Store

# only the landing frame is published; the other texture experiments stay local
background/*
!background/landing.jpg
